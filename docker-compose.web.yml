services:
  web:
    image: ${IMAGE_REGISTRY:-ghcr.io}/lyb88999/v2m-web:latest
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: v2m_web
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "80:80"
